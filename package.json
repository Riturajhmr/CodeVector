{
  "name": "tempo-backend",
  "version": "1.0.0",
  "description": "Cursor-paginated product API — backend take-home assignment",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:init": "node scripts/init-db.js",
    "seed": "node seed.js",
    "test:inserts": "node scripts/test-inserts.js",
    "test:updates": "node scripts/test-updates.js",
    "verify":       "node scripts/verify-pagination.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
